name: sqflite_dev
description: Web-based SQLite workbench for Flutter and pure Dart development
version: 1.0.8
homepage: https://github.com/sbrsubuvga/sqflite-dev
license: MIT

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  sqflite_common: ^2.4.0
  shelf: ^1.4.0
  shelf_cors_headers: ^0.1.5
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # Web UI is embedded directly in the Dart code (no assets needed)

